[package]
name = "isobmff-core"
version = "0.0.0"
description = "Minimal, dependency-free extension layer for ISO base media file format boxes — no_std, alloc optional"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
readme.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true
exclude.workspace = true

[features]
default = ["alloc"]
# Owned boxes — `AnyBox` and `NullTerminatedString` — which need a heap.
alloc = []

[dependencies]

[lints]
workspace = true
